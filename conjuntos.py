numeros = set([1,2,2,3,4,1,5,2])
print(numeros)

fruta = set("abacaxi")
print(fruta)


## Conjuntos não suporttam indexação, por isso é necessário converter em uma lista.
# Função enumerate
# União conjunto_a.union(conjunto_b)
# Intersecção conjunto_a.intersection(conjunto_b)
# Diferença conjunto_a.difference(conjunto_b) 
# Diferença simétrica (todos elementos que nao estão na intersecção) conjunto_a.symetric_difference(conjunto_b)
# Issubset conjunto_a.intersection(conjunto_b) (retorna true ou false) - todos elementos de a estão em b?
#Issuperset 
#Is disjoint 
# .ADD - .clear - .copy - .discard (descartar um valor) - .pop - remove (parecido com o discard) - in (se está)


